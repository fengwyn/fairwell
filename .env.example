SECRET_KEY=django-insecure-CHANGE-ME-FOR-PROD
ALLOWED_HOSTS=localhost,127.0.0.1,gentoo,192.168.0.241

# Dev (SQLite). Default is project root. Point at an external SSD on the Pi
# to keep heavy DB writes off the SD card.
# DEV_DB_PATH=/home/gwyn/lake/fairwell/db.sqlite3

# Prod (Postgres) — used by settings/prod.py.
DB_NAME=fairwell
DB_USER=fairwell
DB_PASSWORD=changeme
DB_HOST=127.0.0.1
DB_PORT=5432
